# 🎈 Proyecto para conocer mis tareas diarias y actualizarlas

1. Install the requirements

   ```
   $ pip install -r requirements.txt
   ```

2. Run the app

   ```
   $ streamlit run streamlit_app.py
   ```
